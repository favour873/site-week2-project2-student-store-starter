{
  "name": "student-store-express-backend",
  "version": "0.0.1",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "dev": "PORT=3001 nodemon server.js --port 3001"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "lowdb": "^1.0.0",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18.16.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "description": "Run `npm install` to install the required dependencies, then run `npm start` to start the backend.",
  "author": "",
  "license": "ISC"
}
